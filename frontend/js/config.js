// Global Frontend Configuration

// The relative URL to the backend API.
// Using a relative path guarantees it will work dynamically
// on BOTH localhost/HomeService and the live server without 404s or CORS errors.
const API_BASE_URL = '../backend/api';

const BACKEND_BASE_URL = '../backend';
